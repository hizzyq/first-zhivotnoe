app:
  env: "local"
  port: "8080"

limits:
  max_size_mb: 20

server:
  timeout: 4s
  idle_timeout: 60s

s3:
  endpoint: "http://localhost:9000"
  access_key: ""
  secret_key: ""
  bucket_name: ""
  use_ssl: false

broker:
  address:
  queue_name: