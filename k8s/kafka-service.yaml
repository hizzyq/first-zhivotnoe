apiVersion: v1
kind: Service
metadata:
  name: kafka-service
spec:
  type: ClusterIP # Изолируем внутри кластера 🔒
  ports:
    - port: 9092
      targetPort: 9092 # Порт внутри контейнера Кафки
  selector:
    app: kafka-broker # Ищет под Кафки по этой метке 🔍